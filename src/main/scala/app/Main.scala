package app

object Main {

}

  //val setUrl =
  /*
  magicSetList
    .foreach(setName => {
      val urlList = buildPaginatedSearchUrls(setName)
      println(urlList)
    })
    */


  /*
  magicSetList
    .map(buildPaginatedSearchUrls)
    .foreach(println)
  */

  /*
  val cardDetails = cardDetailsFromUrl(buildCardUrl("403269"))
  cardDetails.foreach(println)
  */

  /*
  buildPaginatedSearchUrls("Battle for Zendikar")
    .flatMap(cardListFromSet)
    .map(buildCardUrl)
    .map(cardDetailsFromUrl)
    .foreach(set => {println(set); println})
    */
